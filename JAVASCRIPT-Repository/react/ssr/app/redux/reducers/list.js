const defaultState = {
    list: [
        'react1111',
        'react2222',
        'react3333',
    ]
}
export default function(state = defaultState, action){
    switch(action.type){
        default:
            return state
    }
}